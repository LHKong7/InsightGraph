[project]
name = "insightgraph-resolver"
version = "0.1.0"
description = "Entity resolution and deduplication for InsightGraph"
requires-python = ">=3.11"
dependencies = [
    "insightgraph-core",
    "litellm>=1.40",
]

[tool.uv.sources]
insightgraph-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/insightgraph_resolver"]
